export class Card {
  public title: string;
  public date: string;
  public img: String;

  constructor(title: string, date: string, img: string) {
    this.title = title;
    this.date = date;
    this.img = img;
  }
}
